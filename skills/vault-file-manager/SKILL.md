# Skill: Vault File Manager

## Purpose
Moves files between different folders in the Obsidian Vault to manage the task workflow.

## Usage
- Run the script: `python scripts/move_task.py --file "task.md" --from "Inbox" --to "Needs_Action"`
- Valid folders: `Inbox`, `Needs_Action`, `Needs_Approval`, `Done`.
