# Skill: Gmail Send

## Purpose
Sends emails using SMTP with Gmail.

## Requirements
- Environment Variables: `EMAIL_ADDRESS`, `EMAIL_PASSWORD` (App Password).

## Usage
- Provide recipient address (`to`), `subject`, and `body`.
- Run the script: `python scripts/send_email.py --to "recipient@example.com" --subject "Hello" --body "Message here"`
